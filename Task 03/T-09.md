# **Task 9: Merging Branches**
1. Switch back to the `main` branch:  
   ```bash
   git checkout main
   ```
2. Merge the `feature-branch` into `main`:  
   ```bash
   git merge feature-branch
   ```


 ##  ** explain - switch on main branch and merge feature branch with main branch   . **
